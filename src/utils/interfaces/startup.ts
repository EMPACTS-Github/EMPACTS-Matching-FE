export interface Startup {
    id: number;
    name: string;
    description: string;
    category: string;
    location_based: string;
    startup_link: string;
}

export interface Member {
    email: string;
    title: string;
}
